# runtime state / secrets — never commit
config.yaml
secret.key
*.key
nonces.txt
bb/
webdata/
*.offset
orchestrator/logs/*.jsonl
# python
__pycache__/
*.pyc
.venv/
venv/

# CLOUD-ONLY: installed by deploy/install_agent_host.sh on the agent host only.
# Its ABSENCE here is the security property — see deploy/api_runtime/__init__.py.
orchestrator/api_runtime/
